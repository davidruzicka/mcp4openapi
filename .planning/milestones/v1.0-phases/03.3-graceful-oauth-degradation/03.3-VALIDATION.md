---
phase: 03.3
slug: graceful-oauth-degradation
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-19
audited: 2026-05-19
---

# Phase 03.3 — Validation Strategy

> Reconstructed from SUMMARY artifacts (State B — no VALIDATION.md existed pre-audit).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest (per `vitest.config.ts`, includes `src/**/*.test.ts`) |
| **Config file** | `vitest.config.ts` |
| **Quick run command** | `npx vitest run src/auth/oauth-provider.test.ts src/profile/profile-resolver.test.ts src/transport/profile-index.test.ts src/transport/http-transport.test.ts` |
| **Full suite command** | `npm test` (runs `tsc --noEmit` + `vitest run`) |
| **Estimated runtime** | ~5 seconds (quick), ~60 seconds (full) |

---

## Sampling Rate

- **After every task commit:** Run quick command above
- **After every plan wave:** Run `npm test`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** ~60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 03.3-01-01 | 01 | 1 | DEGRADE-01 | unit | `npx vitest run src/auth/oauth-provider.test.ts -t "isOAuthConfigOperational"` | ✅ | ✅ green |
| 03.3-02-01 | 02 | 2 | DEGRADE-01 | integration | `npx vitest run src/transport/http-transport.test.ts -t "oauthDisabledReason"` | ✅ | ✅ green |
| 03.3-02-02 | 02 | 2 | DEGRADE-01 | unit | `npx vitest run src/profile/profile-resolver.test.ts -t "filters OAuth"` | ✅ | ✅ green |
| 03.3-02-03 | 02 | 2 | DEGRADE-01 | contract | `npx vitest run src/transport/profile-index.test.ts -t "oauth authTab"` | ✅ | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] `src/auth/oauth-provider.ts` — exports `isOAuthConfigOperational`, `OAuthOperationalCheck`; module-private `tryResolveEnvRef`
- [x] `src/auth/oauth-provider.test.ts` — 15+ parametrized cases covering all `isOAuthConfigOperational` branches (operational true/false, env var resolution, allow_unregistered_clients)
- [x] `src/transport/http-transport.ts` — `oauthDisabledReason?: string` in `ProfileRuntimeState`; pre-flight check in `getProfileState()`; auth gate guard `!profileState.oauthDisabledReason`
- [x] `src/transport/http-transport.test.ts` — `oauthDisabledReason` set, `oauthProvider` null, warn logged; HTTP degradation behavior tests
- [x] `src/profile/profile-resolver.ts` — `extractAuthMethods()` filters OAuth when `isOAuthConfigOperational` returns false
- [x] `src/profile/profile-resolver.test.ts` — OAuth filtered from authMethods when env var unresolved; kept when config complete
- [x] `src/transport/profile-index.test.ts` — no oauth authTab emitted when OAuth absent from authMethods

*Framework install not needed — Vitest already configured.*

---

## Requirement Coverage

| Requirement | Description | Tests | Status |
|-------------|-------------|-------|--------|
| DEGRADE-01 | OAuth pre-flight check: degrade gracefully when config incomplete | `oauth-provider.test.ts` describe('isOAuthConfigOperational') — 15+ cases; `http-transport.test.ts` oauthDisabledReason + HTTP degradation; `profile-resolver.test.ts` filtering; `profile-index.test.ts` tab suppression | **COVERED** |

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| HTML index hides OAuth tab in browser when env vars missing | DEGRADE-01 | Client-side tab rendering not covered by Vitest | Start server with `auth.type=oauth` but `MCP4_OAUTH_ISSUER` unset; open index page; verify OAuth auth tab absent, bearer tab present |

---

## Audit Notes (2026-05-19)

- **State B reconstruction** — no VALIDATION.md existed; reconstructed from 03.3-01-SUMMARY.md + 03.3-02-SUMMARY.md
- **393 tests ran** across 4 files; all pass
- **No gaps found** — all DEGRADE-01 behaviors have automated test coverage
- **15+ isOAuthConfigOperational cases** in oauth-provider.test.ts cover all branches
- **Fixture fixes** in http-transport.test.ts (3 fixtures) and http-transport-security.test.ts (10 fixtures) added `redirect_uri` to prevent false degradation in unrelated tests — these are correctness fixes, not gap coverage

---

## Validation Sign-Off

- [x] All tasks have automated verify command — 4/4 tasks covered
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all required artifacts — 7 files verified
- [x] No watch-mode flags
- [x] Feedback latency < 60s (quick run: 4.82s)
- [x] `nyquist_compliant: true` set in frontmatter

**Audited:** 2026-05-19 — 393 tests across 4 files, 0 failures, 0 gaps found
**Approval:** PASSED
