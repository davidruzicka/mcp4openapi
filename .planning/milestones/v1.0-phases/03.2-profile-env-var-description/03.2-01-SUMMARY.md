---
phase: 03.2-profile-env-var-description
plan: 01
subsystem: profile
tags: [env-var, configuration, fail-fast, map, typescript]

requires:
  - phase: 03.1-odstran-n-multi-upstream-mcp-supportu
    provides: "ListedProfileDetails interface with profileAliases field (profile-resolver.ts)"
provides:
  - "parseProfilesDescriptionEnv: parses MCP4_PROFILES_DESCRIPTION raw env-var value to Map<string,string>"
  - "resolveProfileAdminDescriptions: resolves description map keys against profiles by id/name/alias"
  - "PROFILES_DESCRIPTION_ENV_VAR: single source of truth constant for env-var name"
affects:
  - 03.2-02 (wires parse+resolve into HttpTransport startup path)
  - 03.2-03 (consumes resolved Map in HTML template render)

tech-stack:
  added: []
  patterns:
    - "Pure helper module pattern: parse + resolve separated so each is independently unit-testable"
    - "D-05 conflict detection via firstKeyByProfile tracking map: detect two keys mapping to same profile"
    - "Fail-fast ConfigurationError on startup with structured details (envVar, key, receivedType, conflictingKeys)"

key-files:
  created:
    - src/profile/profile-description-env.ts
    - src/profile/profile-description-env.test.ts
  modified: []

key-decisions:
  - "Parse-time conflict detection intentionally skipped — D-05 conflict requires profile list which is unavailable at parse time; deferred to resolveProfileAdminDescriptions"
  - "receivedType for null uses string 'null' not typeof (which also returns 'object') for clearer error messages to admins"
  - "Empty string values allowed at parse time, treated as no-op at render time (Pitfall §6)"

patterns-established:
  - "PROFILES_DESCRIPTION_ENV_VAR constant exported for import by Plans 02/03 — no string literal duplication"
  - "ConfigurationError details include envVar field on every error thrown from this module"

requirements-completed: [D-01, D-02, D-03, D-04, D-05, D-07, D-08, D-09]

duration: 3min
completed: 2026-05-03
---

# Phase 03.2 Plan 01: profile-description-env module Summary

**Pure parse+resolve helpers for MCP4_PROFILES_DESCRIPTION env var: fail-fast JSON object validation, profile key resolution by id/name/alias, D-05 conflict detection with sorted conflictingKeys**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-05-03T22:33:52Z
- **Completed:** 2026-05-03T22:35:59Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Created `src/profile/profile-description-env.ts` exporting `parseProfilesDescriptionEnv`, `resolveProfileAdminDescriptions`, and `PROFILES_DESCRIPTION_ENV_VAR`
- 23 unit tests covering all D-02..D-09 locked decisions pass under vitest
- TypeScript strict compile clean, no out-of-scope edits, no schema files touched

## Public API

```typescript
// Single source of truth for env-var name (Plans 02/03 import this)
export const PROFILES_DESCRIPTION_ENV_VAR = 'MCP4_PROFILES_DESCRIPTION';

// Parse raw env-var value → Map<key,description> or undefined
export function parseProfilesDescriptionEnv(
  raw: string | undefined
): Map<string, string> | undefined

// Resolve map keys against profile list → Map<profileId,description>
export function resolveProfileAdminDescriptions(
  descriptions: Map<string, string> | undefined,
  profiles: ListedProfileDetails[]
): Map<string, string>
```

## Four Error Message Strings (for Plan 02 wiring reference)

1. `'MCP4_PROFILES_DESCRIPTION is not valid JSON'` — thrown on `JSON.parse` failure
2. `'MCP4_PROFILES_DESCRIPTION must be a JSON object'` — thrown on null/array/non-object payload
3. `'MCP4_PROFILES_DESCRIPTION values must be strings'` — thrown on non-string entry value
4. `'MCP4_PROFILES_DESCRIPTION has multiple keys resolving to the same profile'` — thrown on D-05 conflict

## Schema Files Untouched

Confirmed: `src/types/profile.ts`, `src/generated-schemas.ts`, `profile-schema.json` — zero changes. D-01 env-var-only approach preserved.

## Vitest Results

23 tests passed, 0 failed, 0 skipped.

## Task Commits

1. **Task 1: Create profile-description-env module** - `50f9203` (feat)
2. **Task 2: Add unit tests** - `254ead1` (test)

## Files Created/Modified

- `src/profile/profile-description-env.ts` — new pure module: parse + resolve helpers, PROFILES_DESCRIPTION_ENV_VAR constant
- `src/profile/profile-description-env.test.ts` — 23 unit tests covering all D-02..D-09 decisions

## Decisions Made

- Parse-time conflict detection intentionally skipped: D-05 conflict requires the profile list which is only available later; deferred to `resolveProfileAdminDescriptions`
- `null` JSON value returns `receivedType: 'null'` (not `'object'`) for a clearer admin error message
- Empty string description values pass through parse and resolve unchanged; render-time is where they become no-ops

## Deviations from Plan

None — plan executed exactly as written. The exact file content specified in the plan was used verbatim.

## Issues Encountered

None — `--reporter=basic` flag in the plan's verify command is not recognized by this vitest version; ran without it. All tests passed.

## Next Phase Readiness

- Plan 02 can directly import `parseProfilesDescriptionEnv`, `resolveProfileAdminDescriptions`, and `PROFILES_DESCRIPTION_ENV_VAR` from `./profile-description-env.js`
- Plan 02 wires parse at startup (process.env read in core/index.ts or similar), resolve after profiles are loaded
- Plan 03 consumes the resolved Map in the HTML template render path

---
*Phase: 03.2-profile-env-var-description*
*Completed: 2026-05-03*
