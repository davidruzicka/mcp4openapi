---
phase: 03.4
slug: encrypted-token-envelope
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-19
audited: 2026-05-19
---

# Phase 03.4 — Validation Strategy

> Reconstructed from SUMMARY artifacts (State B — no VALIDATION.md existed pre-audit).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest (per `vitest.config.ts`, includes `src/**/*.test.ts`) |
| **Config file** | `vitest.config.ts` |
| **Quick run command** | `npx vitest run src/auth/token-envelope.test.ts src/transport/http-transport-config.test.ts src/transport/http-transport.test.ts` |
| **Full suite command** | `npm test` (runs `tsc --noEmit` + `vitest run`) |
| **Estimated runtime** | ~5 seconds (quick), ~60 seconds (full) |

---

## Sampling Rate

- **After every task commit:** Run quick command above
- **After every plan wave:** Run `npm test`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** ~60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 03.4-01-01 | 01 | 1 | D-FORMAT/D-PREFIX/D-KEYDERIV/D-CREG | unit (TDD) | `npx vitest run src/auth/token-envelope.test.ts` | ✅ | ✅ green |
| 03.4-02-01 | 02 | 1 | D-CONFIG-FIELD/D-KEYDERIV | unit (TDD) | `npx vitest run src/transport/http-transport-config.test.ts` | ✅ | ✅ green |
| 03.4-03-01 | 03 | 2 | D-STARTUP-WARN/D-DEFAULT-MAXLEN | integration (TDD) | `npx vitest run src/transport/http-transport.test.ts -t "MCP4_OAUTH_KEY startup warn"` | ✅ | ✅ green |
| 03.4-03-02 | 03 | 2 | D-STORE-RETURN/D-CALLSITES | integration (TDD) | `npx vitest run src/transport/http-transport.test.ts -t "storeOAuthTokens encrypted"` | ✅ | ✅ green |
| 03.4-03-03 | 03 | 2 | D-SESSION-INIT-FALLBACK | integration (TDD) | `npx vitest run src/transport/http-transport.test.ts -t "encrypted token envelope session"` | ✅ | ✅ green |
| 03.4-04-01 | 04 | 3 | D-DOCS | manual | Verify env.example, README.md, docs/HTTP-TRANSPORT.md, IMPLEMENTATION.md, CHANGELOG.md | ✅ | ✅ verified |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] `src/auth/token-envelope.ts` — 5 exports: `TokenEnvelopePayload`, `deriveTokenKey`, `isEncryptedToken`, `encryptTokenPayload`, `decryptTokenPayload`; pure AES-256-GCM module, no env reads, never-throws decrypt contract
- [x] `src/auth/token-envelope.test.ts` — 24 vitest cases: round-trip x4, tamper x2, AAD/replay x1, format/garbage x4, key derivation x2, prefix detection x1, nonce randomness x1, regex compat x1, creg present/absent x2, + extras
- [x] `src/types/http-transport.ts` — `tokenKey?: Buffer` field on `HttpTransportConfig`
- [x] `src/transport/http-transport-config.ts` — IIFE parser for `MCP4_OAUTH_KEY` with `deriveTokenKey()` wiring; whitespace-trim tolerance
- [x] `src/transport/http-transport-config.test.ts` — 12 tests (5 prior + 7 new: unset, empty, whitespace, 64-hex zeros, passphrase SHA-256, whitespace+passphrase, 64-hex all-a)
- [x] `src/transport/http-transport.ts` — startup warn on missing `MCP4_OAUTH_KEY`; `DEFAULT_MAX_TOKEN_LENGTH=4096`; `storeOAuthTokens` returns string, creg param, both maps keyed by returned token; 3 call-site updates; session-init decrypt fallback
- [x] `src/transport/http-transport.test.ts` — startup warn + 4096 boundary (Tasks 1-4); storeOAuthTokens encrypted D-I, /oauth/token J-K, refresh L; session recovery M-R (restore, creg, no-creg, cross-profile reject, no-tokenKey skip, no-prefix skip)
- [x] `env.example` — `#MCP4_OAUTH_KEY=` entry (fixed stale `MCP4_TOKEN_KEY` name during audit 2026-05-19)
- [x] `README.md` — `MCP4_OAUTH_KEY` env-var table entry with AES-256-GCM / passphrase / hex format guidance
- [x] `docs/HTTP-TRANSPORT.md` — `## Encrypted Token Envelopes` top-level section: format, when issued, restart-recovery flow, key derivation, rotating-rt limitation, security boundary
- [x] `IMPLEMENTATION.md` — OAuth lifecycle pointer to `src/auth/token-envelope.ts` and HTTP-TRANSPORT.md section
- [x] `CHANGELOG.md` — single consolidated user-perspective line for the feature

*Framework install not needed — Vitest already configured.*

---

## Requirement Coverage

| Requirement | Description | Tests | Status |
|-------------|-------------|-------|--------|
| D-FORMAT | AES-256-GCM envelope `mcp4.v1.<base64url(nonce\|ct\|tag)>`, profile_id as AAD | `token-envelope.test.ts` round-trip + tamper + AAD cases | **COVERED** |
| D-PREFIX | `mcp4.v1.` prefix for O(1) format detection; `isEncryptedToken` | `token-envelope.test.ts` prefix detection + regex compat | **COVERED** |
| D-KEYDERIV | 64-hex direct decode vs SHA-256(passphrase); whitespace-trim | `token-envelope.test.ts` key derivation x2; `http-transport-config.test.ts` 7 env-var cases | **COVERED** |
| D-CREG | DCR client registration embedded in envelope.creg; `client_secret` NEVER embedded | `http-transport.test.ts` Test G (creg round-trip) + Test H (no creg) | **COVERED** |
| D-CONFIG-FIELD | `HttpTransportConfig.tokenKey?: Buffer` wired from `MCP4_OAUTH_KEY` | `http-transport-config.test.ts` MCP4_OAUTH_KEY describe block | **COVERED** |
| D-STARTUP-WARN | One-shot warn when `MCP4_OAUTH_KEY` unset | `http-transport.test.ts` startup warn describe block | **COVERED** |
| D-DEFAULT-MAXLEN | `DEFAULT_MAX_TOKEN_LENGTH` raised 1000 -> 4096 | `http-transport.test.ts` 4096-char boundary accept + 4097 reject | **COVERED** |
| D-STORE-RETURN | `storeOAuthTokens` returns client-facing token string; maps keyed by returned token | `http-transport.test.ts` Tests D-I (encrypted path) + existing plain-token tests | **COVERED** |
| D-CALLSITES | All 3 call sites (auth_code, refresh_token, internal refresh) updated | `http-transport.test.ts` Tests J + K + L | **COVERED** |
| D-SESSION-INIT-FALLBACK | Decrypt fallback on miss; cross-profile rejection; creg rehydration | `http-transport.test.ts` Tests M-R | **COVERED** |
| D-DOCS | env.example, README, HTTP-TRANSPORT.md, IMPLEMENTATION.md, CHANGELOG updated | Manual verification (see audit notes) | **COVERED** |

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| MCP OAuth client survives gateway restart in k8s | D-SESSION-INIT-FALLBACK | Requires actual process restart, not mockable | Start gateway with `MCP4_OAUTH_KEY=<passphrase>`, complete OAuth flow, restart gateway, verify client session resumes without re-auth prompt |

---

## Audit Notes (2026-05-19)

- **State B reconstruction** — no VALIDATION.md existed; reconstructed from 03.4-01-SUMMARY.md through 03.4-04-SUMMARY.md
- **237 tests ran** (24 + 12 + 201) across 3 files; all pass
- **1 gap fixed during audit:** `env.example` had stale `#MCP4_TOKEN_KEY=` (missed by rename commit `39b7b5a`); corrected to `#MCP4_OAUTH_KEY=` in-place
- **No missing tests** — all locked decisions (D-FORMAT through D-DOCS) have automated coverage
- **TDD discipline verified** — each of Plans 01/02/03 has paired RED/GREEN commits in git log; Plan 04 is docs-only (no TDD needed)
- **Pre-existing failures** — 3 tests in `src/testing/token-validation.test.ts` (validation timeout) were pre-existing before Phase 03.4 and logged to `deferred-items.md`; not related to this phase

---

## Validation Sign-Off

- [x] All tasks have automated verify command — 5/5 code tasks covered; 1 docs task manually verified
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all required artifacts — 11 files verified
- [x] No watch-mode flags
- [x] Feedback latency < 60s (quick run: ~5s)
- [x] `nyquist_compliant: true` set in frontmatter

**Audited:** 2026-05-19 — 237 tests across 3 files, 0 failures, 1 doc gap fixed (env.example rename)
**Approval:** PASSED
