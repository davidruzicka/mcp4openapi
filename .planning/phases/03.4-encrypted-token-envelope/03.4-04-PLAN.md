---
phase: 03.4-encrypted-token-envelope
plan: 04
type: execute
wave: 4
depends_on:
  - 03.4-03
files_modified:
  - env.example
  - README.md
  - docs/HTTP-TRANSPORT.md
  - IMPLEMENTATION.md
  - CHANGELOG.md
autonomous: true
requirements:
  - DECISION-D-DOCS
must_haves:
  truths:
    - "env.example documents MCP4_TOKEN_KEY (as a commented optional setting) immediately after the MCP4_TOKEN_MAX_LENGTH line"
    - "README.md env-var table includes MCP4_TOKEN_KEY with description, default behavior (plain-token mode when unset), and acceptable formats (any passphrase or 64-char hex)"
    - "docs/HTTP-TRANSPORT.md has a new 'Encrypted Token Envelopes' section explaining the mcp4.v1.* format, when envelopes are issued, the restart-resilience guarantee, the rotating-rt limitation, and the security boundary (AAD = profile_id, no client_secret embedded)"
    - "IMPLEMENTATION.md OAuth token lifecycle subsection mentions the encrypted envelope path: that storeOAuthTokens issues mcp4.v1.* envelopes when MCP4_TOKEN_KEY is set, that session init can rehydrate from envelope after restart, and that the in-memory maps are keyed by the issued (potentially encrypted) token"
    - "CHANGELOG.md has a single one-line entry under [Unreleased] -> ### Added describing the encrypted token envelope feature in user-perspective terms"
  artifacts:
    - path: "env.example"
      provides: "MCP4_TOKEN_KEY commented-out example with usage hint"
      contains: "MCP4_TOKEN_KEY"
    - path: "README.md"
      provides: "MCP4_TOKEN_KEY entry in env-var table"
      contains: "MCP4_TOKEN_KEY"
    - path: "docs/HTTP-TRANSPORT.md"
      provides: "'Encrypted Token Envelopes' section"
      contains: "Encrypted Token Envelopes"
    - path: "IMPLEMENTATION.md"
      provides: "encrypted envelope path noted in OAuth token lifecycle description"
      contains: "mcp4.v1"
    - path: "CHANGELOG.md"
      provides: "one-line Added entry for encrypted token envelopes"
      contains: "encrypted token"
  key_links:
    - from: "env.example"
      to: "README.md MCP4_TOKEN_KEY entry"
      via: "matching env var name and description"
      pattern: "MCP4_TOKEN_KEY"
    - from: "docs/HTTP-TRANSPORT.md Encrypted Token Envelopes section"
      to: "src/auth/token-envelope.ts"
      via: "format spec mcp4.v1.<base64url(nonce + ciphertext + tag)> matches the implementation"
      pattern: "mcp4\\.v1"
---

<objective>
Document the MCP4_TOKEN_KEY env var and the encrypted token envelope feature across the user-facing
docs (env.example, README.md, docs/HTTP-TRANSPORT.md), the architectural reference (IMPLEMENTATION.md),
and the changelog (CHANGELOG.md).

Purpose: Operators deploying mcp4openapi in k8s need to understand (a) why the feature exists
(restart-resilient OAuth without persistent storage), (b) how to enable it (one env var), and
(c) what backward-compat guarantees they get when leaving it unset. Developers reading
IMPLEMENTATION.md need the architectural pointer to find the new code path.

Output: Edits to 5 documentation files. No code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.4-encrypted-token-envelope/03.4-CONTEXT.md
@.planning/phases/03.4-encrypted-token-envelope/03.4-01-SUMMARY.md
@.planning/phases/03.4-encrypted-token-envelope/03.4-02-SUMMARY.md
@.planning/phases/03.4-encrypted-token-envelope/03.4-03-SUMMARY.md
@AGENTS.md
@env.example
@README.md
@docs/HTTP-TRANSPORT.md
@IMPLEMENTATION.md
@CHANGELOG.md
</context>

<interfaces>
<!-- This plan touches only docs. The interfaces it documents are: -->

Env var contract (Plan 02):
- `MCP4_TOKEN_KEY` (optional)
  - Unset: plain-token mode, OAuth clients re-auth on every gateway restart, startup warn emitted.
  - 64-char hex string: decoded as 32 raw bytes (AES-256 key).
  - Anything else: SHA-256(value) is used as the key (any passphrase works).
  - Whitespace around the value is trimmed before derivation.

Token format (Plan 01):
- `mcp4.v1.<base64url(12-byte-nonce + AES-256-GCM-ciphertext + 16-byte-tag)>`
- AAD = profile_id (UTF-8 bytes) - prevents cross-profile replay.
- Payload contains: access_token, refresh_token (optional), expiry, client_id, scopes,
  profile_id, issued-at, optional client registration snapshot (creg). client_secret is NEVER
  embedded.

Existing CHANGELOG.md style (from `[Unreleased]` -> `### Added` examples in the file):
- ONE LINE per change from a user perspective. Compress multi-aspect changes into a single line
  per AGENTS.md "Update CHANGELOG.md ... Always compress lines."

Existing env.example anchor (line 74):
```
#MCP4_TOKEN_MAX_LENGTH=1000  # Maximum bearer token length in characters
```

Existing README.md anchor (line 567):
```
- `MCP4_TOKEN_MAX_LENGTH`: Maximum token length in characters (default: `1000`)
```

Existing docs/HTTP-TRANSPORT.md structure: top-level `## ` sections including "Session Management"
(line 554) and "SSE Resumability" (line 611). The new "Encrypted Token Envelopes" section
belongs near "Session Management" because it materially changes session-init behavior across
restarts.

Existing IMPLEMENTATION.md OAuth bullets (lines 275-277):
```
- Session management with UUID, 30min timeout default (OAuth sessions: 24h default)
- **OAuth session auto-refresh**: `HttpTransport` stores refresh tokens in `SessionData` and
  automatically refreshes expired access tokens via `ensureValidSessionToken()` before outbound API calls
- OAuth sessions have extended timeout policy (24h default, configurable) vs static token sessions (30min)
```
The new bullet (or extension of an existing one) goes immediately after this group.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update env.example, README.md, docs/HTTP-TRANSPORT.md, IMPLEMENTATION.md, CHANGELOG.md for MCP4_TOKEN_KEY and encrypted token envelopes</name>
  <files>env.example, README.md, docs/HTTP-TRANSPORT.md, IMPLEMENTATION.md, CHANGELOG.md</files>
  <read_first>
    - env.example (lines 70-80 - context around MCP4_TOKEN_MAX_LENGTH at line 74)
    - README.md (lines 555-585 - context around MCP4_TOKEN_MAX_LENGTH at line 567 in the env-var list/table)
    - docs/HTTP-TRANSPORT.md (lines 540-610 - Session Management section to anchor the new section near it)
    - IMPLEMENTATION.md (lines 270-290 - OAuth token lifecycle bullets)
    - CHANGELOG.md (lines 1-40 - current [Unreleased] section style; prove the one-line-per-user-change convention)
    - AGENTS.md (the "## Avoid" + "Update CHANGELOG.md" guidance: compress lines, no em/en dashes)
  </read_first>
  <action>
**Edit 1: env.example - add MCP4_TOKEN_KEY entry after line 74.**

After the existing line 74:
```
#MCP4_TOKEN_MAX_LENGTH=1000  # Maximum bearer token length in characters
```

Add (verbatim, on a new line):
```
#MCP4_TOKEN_KEY=                          # Optional: enables AES-256-GCM encrypted token envelopes (mcp4.v1.*) for restart-resilient OAuth sessions. Accepts any passphrase, or a 64-char hex string for a raw 32-byte key. When unset, OAuth clients must re-authenticate after every gateway restart.
```

Use exactly two spaces after `#MCP4_TOKEN_KEY=` and before the comment. Do NOT add any
non-ASCII characters (no em/en dashes - use ASCII hyphen-minus or comma).

**Edit 2: README.md - add MCP4_TOKEN_KEY entry after line 567.**

After the existing line 567:
```
- `MCP4_TOKEN_MAX_LENGTH`: Maximum token length in characters (default: `1000`)
```

Add as a new bullet:
```
- `MCP4_TOKEN_KEY`: Optional symmetric key for AES-256-GCM encrypted token envelopes. When set, the gateway issues `mcp4.v1.*` tokens to OAuth clients on `/oauth/token` and rehydrates sessions from them on restart, eliminating the re-authentication round-trip in k8s restart scenarios. Accepts any passphrase (SHA-256-derived) or a 64-char hex string (32 raw bytes). Default unset (plain-token mode, backward-compatible). See `docs/HTTP-TRANSPORT.md` -> Encrypted Token Envelopes for details.
```

Note: `MCP4_TOKEN_MAX_LENGTH` default in README still says `1000` but Plan 03 raised the
runtime default to 4096. UPDATE that line as part of this task to:
```
- `MCP4_TOKEN_MAX_LENGTH`: Maximum token length in characters (default: `4096`, raised from `1000` in Phase 03.4 to accommodate encrypted token envelopes)
```

**Edit 3: docs/HTTP-TRANSPORT.md - add "Encrypted Token Envelopes" section.**

Insert a NEW top-level `## Encrypted Token Envelopes` section IMMEDIATELY AFTER the
"Session Storage" subsection (which ends near line 611, just before "## SSE Resumability").
The section content:

```markdown
## Encrypted Token Envelopes

When `MCP4_TOKEN_KEY` is configured, the gateway wraps OAuth tokens in encrypted envelopes so MCP
clients can survive arbitrary gateway restarts (for example k8s pod evictions) without re-running
the OAuth browser flow.

### Token format

```
mcp4.v1.<base64url(12-byte-nonce + AES-256-GCM-ciphertext + 16-byte-tag)>
```

- Algorithm: AES-256-GCM with a fresh 12-byte random nonce per token.
- Additional Authenticated Data (AAD): the profile_id as UTF-8 bytes. This binds an envelope to
  exactly one profile and prevents cross-profile replay.
- Payload (encrypted): IdP access_token, IdP refresh_token (optional), expiry, OAuth client_id,
  scopes, profile_id, issued-at timestamp, and an optional OAuth client registration snapshot
  (creg) so the client does not need to re-register on restart.
- `client_secret` is NEVER embedded - DCR clients are public PKCE clients without one.

### When envelopes are issued

The gateway returns an envelope as `access_token` in the `/oauth/token` response only when ALL of:
1. `MCP4_TOKEN_KEY` is configured at startup.
2. The IdP returned a `refresh_token` (envelopes without a refresh path provide no recovery
   benefit, so plain access_token is returned instead).

If encryption fails for any reason, the gateway logs a warn and falls back to the plain IdP
access_token - the response shape is unchanged. Clients ALWAYS work, with or without envelopes.

### Restart-recovery flow

1. The MCP client stores the `mcp4.v1.*` token from the OAuth response.
2. The gateway is restarted (k8s rolling deploy, OOM kill, etc.) and all in-memory state is lost.
3. The client reconnects and re-presents the same envelope on the next MCP `initialize` request.
4. The gateway detects the `mcp4.v1.` prefix, decrypts using `MCP4_TOKEN_KEY` and the
   request-profile_id (as AAD). On success, it rehydrates the session: refresh_token, expiry,
   client_id, scopes, and (if creg is present) the OAuth client registration in memory.
5. If the access token is already expired, the existing refresh-token path silently exchanges
   it for a fresh access token in the next request - the client sees no auth challenge.

### Key derivation (`MCP4_TOKEN_KEY`)

- 64-char hex string: decoded directly as 32 raw bytes (AES-256 key).
- Anything else: SHA-256(value) yields 32 bytes - any passphrase works.
- Whitespace around the value is trimmed before derivation (k8s ConfigMap newline tolerance).
- Unset: plain-token mode is active and a startup warn is logged. Behavior matches earlier
  releases byte-for-byte.

### Limitation: rotating refresh tokens

If your IdP issues rotating refresh tokens AND the gateway restarts after at least one in-session
token refresh, the client still holds the original envelope with the now-stale `rt`. Re-auth is
required in that case (same as today). For non-rotating refresh tokens, zero-reauth across
arbitrary restarts is supported.

### Security boundary

- The AES-GCM auth tag IS the integrity signature - any tamper to nonce, ciphertext, or tag
  produces a `null` decrypt result, NOT a partial recovery.
- profile_id is bound as AAD AND post-decrypt-validated - even an attacker who possessed the
  symmetric key could not present a profile-A envelope to profile-B.
- All decrypt failures are silent (debug log only) - the session falls back to plain-bearer
  treatment without a 500 or 401.
```

If the existing file already uses a different markdown heading depth at that location (for
example if "Session Storage" is `### Session Storage`, then the surrounding sections may use
`##` for top-level), inspect the file structure and match the depth used by neighboring
top-level sections like "Session Management" (`##`).

**Edit 4: IMPLEMENTATION.md - extend the OAuth token lifecycle bullets (after line 277).**

After the existing line 277:
```
- OAuth sessions have extended timeout policy (24h default, configurable) vs static token sessions (30min)
```

Add a new bullet:
```
- **Restart-resilient OAuth via encrypted token envelopes**: when `MCP4_TOKEN_KEY` is set, `storeOAuthTokens()` issues `mcp4.v1.*` envelopes (AES-256-GCM, profile_id as AAD) carrying access/refresh/expiry/client_id/scopes/optional creg snapshot; in-memory token maps are keyed by the issued envelope (not the raw IdP access_token); session init detects the `mcp4.v1.` prefix and rehydrates session metadata + DCR client registration directly from the envelope on gateway restart. See `src/auth/token-envelope.ts` and `docs/HTTP-TRANSPORT.md` -> Encrypted Token Envelopes.
```

**Edit 5: CHANGELOG.md - add ONE LINE entry under `[Unreleased]` -> `### Added`.**

Find the `## [Unreleased]` section, then the `### Added` subsection. Add the new entry as the
FIRST bullet under `### Added` (most recent at top, matching the file's convention of newest
on top - confirm by reading neighboring bullets):

```
- Encrypted token envelopes (`mcp4.v1.*`, AES-256-GCM, profile_id as AAD) for restart-resilient OAuth: when `MCP4_TOKEN_KEY` is set, OAuth clients survive k8s pod restarts without re-authentication; backward-compatible (unset = plain-token mode + startup warn).
```

This is ONE LINE per AGENTS.md ("Always compress lines"). Do NOT split into multiple bullets
for the module, the wiring, the docs, etc. From the user perspective it is one feature.

**Common rules across all 5 edits:**
- Use ASCII hyphen-minus (`-`) only. NO Unicode em dash (U+2014) or en dash (U+2013) anywhere.
  Per AGENTS.md "Avoid": "Never use the Unicode em dash or en dash". Search the new text after
  edit and replace any if accidentally introduced.
- Do not add a trailing newline beyond what the file already uses (most files end with a single
  `\n`; preserve that).
- All English text. No emojis (the project uses no emojis in docs).
  </action>
  <verify>
    <automated>grep -n "MCP4_TOKEN_KEY" env.example README.md ; grep -n "Encrypted Token Envelopes" docs/HTTP-TRANSPORT.md ; grep -n "mcp4.v1" IMPLEMENTATION.md CHANGELOG.md ; LC_ALL=C grep -lP "[\\xe2][\\x80][\\x93\\x94]" env.example README.md docs/HTTP-TRANSPORT.md IMPLEMENTATION.md CHANGELOG.md 2>/dev/null || echo "no em/en dash"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^#MCP4_TOKEN_KEY=" env.example` returns 1 (commented-out by convention)
    - `grep -c "MCP4_TOKEN_KEY" README.md` returns at least 1 with description text including "AES-256-GCM" and "passphrase" or "hex"
    - `grep "MCP4_TOKEN_MAX_LENGTH" README.md` shows the updated default is `4096` (not `1000`)
    - `grep -c "^## Encrypted Token Envelopes" docs/HTTP-TRANSPORT.md` returns 1 (new top-level section)
    - `grep -c "mcp4\\.v1\\." docs/HTTP-TRANSPORT.md` returns at least 2 (format spec + flow description)
    - `grep "AAD" docs/HTTP-TRANSPORT.md` matches at least once (security boundary explained)
    - `grep "rotating refresh tokens" docs/HTTP-TRANSPORT.md` matches at least once (limitation documented)
    - `grep "Restart-resilient OAuth via encrypted token envelopes" IMPLEMENTATION.md` matches at least once
    - `grep "src/auth/token-envelope.ts" IMPLEMENTATION.md` matches at least once (developer pointer)
    - `grep "Encrypted token envelopes" CHANGELOG.md` matches at least once under [Unreleased]/Added
    - `grep -c "Encrypted token envelopes" CHANGELOG.md` returns exactly 1 (single line per AGENTS.md - not split)
    - `LC_ALL=C grep -lP "[\\xe2][\\x80][\\x93\\x94]" env.example README.md docs/HTTP-TRANSPORT.md IMPLEMENTATION.md CHANGELOG.md` returns no matches in any new content (no em/en dash introduced; pre-existing matches outside touched regions are acceptable)
    - `grep "client_secret" docs/HTTP-TRANSPORT.md` matches at least once (the section explicitly notes client_secret is NEVER embedded - this is the one place client_secret is mentioned to clarify the security boundary)
    - Commit: `docs(03.4): document MCP4_TOKEN_KEY and encrypted token envelope feature`
  </acceptance_criteria>
  <done>
    - All 5 docs files updated
    - env.example has MCP4_TOKEN_KEY commented entry; README.md table includes it (and bumps MCP4_TOKEN_MAX_LENGTH default to 4096)
    - docs/HTTP-TRANSPORT.md has the new "Encrypted Token Envelopes" section with format, flow, key derivation, limitation, and security boundary
    - IMPLEMENTATION.md OAuth lifecycle has the envelope bullet pointing at src/auth/token-envelope.ts
    - CHANGELOG.md has one compressed line under [Unreleased]/### Added
    - Files committed
  </done>
</task>

</tasks>

<verification>
1. `grep "MCP4_TOKEN_KEY" env.example README.md` - matches both files
2. `grep "Encrypted Token Envelopes" docs/HTTP-TRANSPORT.md` - matches once (new section heading)
3. `grep "mcp4\\.v1" docs/HTTP-TRANSPORT.md IMPLEMENTATION.md` - both files reference the format
4. `grep "encrypted token envelope" CHANGELOG.md` - matches under [Unreleased]/### Added
5. `LC_ALL=C grep -P "[\\xe2][\\x80][\\x93\\x94]"` against new lines - returns nothing (no em/en dash)
6. Total line additions across all 5 files: roughly 60-80 lines (one section in HTTP-TRANSPORT, one bullet each in env.example/README/IMPLEMENTATION/CHANGELOG)
</verification>

<success_criteria>
- Operators can find MCP4_TOKEN_KEY in env.example and README.md and understand when to set it
- The HTTP-TRANSPORT.md "Encrypted Token Envelopes" section explains the format, when envelopes are issued, the restart-recovery flow, key derivation rules, the rotating-rt limitation, and the security boundary (AAD binding, no client_secret embedded, silent failure)
- IMPLEMENTATION.md points developers at src/auth/token-envelope.ts and the docs section
- CHANGELOG.md has one compressed user-perspective entry per AGENTS.md convention
- No Unicode em or en dashes introduced anywhere in the new text
- README.md MCP4_TOKEN_MAX_LENGTH default is updated from 1000 to 4096 to match the runtime change shipped in Plan 03
</success_criteria>

<output>
After completion, create `.planning/phases/03.4-encrypted-token-envelope/03.4-04-SUMMARY.md`
</output>
