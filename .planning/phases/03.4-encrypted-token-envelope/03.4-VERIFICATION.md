---
phase: 03.4-encrypted-token-envelope
verified: 2026-05-06T09:40:00Z
status: passed
score: 4/4 plans verified, all must-haves satisfied
re_verification: false
---

# Phase 03.4: Encrypted Token Envelope Verification Report

**Phase Goal:** Implement encrypted token envelopes (AES-256-GCM, mcp4.v1.* format) so OAuth clients survive gateway restarts without re-authentication. When MCP4_TOKEN_KEY is set, /oauth/token issues encrypted envelopes; session-init fallback decrypts and rehydrates sessions. Full backward-compat when unset.
**Verified:** 2026-05-06T09:40:00Z
**Status:** PASSED
**Re-verification:** No - initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `src/auth/token-envelope.ts` exports `TokenEnvelopePayload`, `deriveTokenKey`, `isEncryptedToken`, `encryptTokenPayload`, `decryptTokenPayload` | VERIFIED | 5 named exports confirmed at lines 42, 67, 78, 88, 125 of token-envelope.ts |
| 2 | `encryptTokenPayload` returns string starting with `mcp4.v1.` using AES-256-GCM with 12-byte nonce and AAD | VERIFIED | `createCipheriv('aes-256-gcm', ...)`, `randomBytes(NONCE_BYTES)`, `cipher.setAAD(Buffer.from(payload.pid, 'utf8'))` all present; TOKEN_PREFIX = `mcp4.v1.` |
| 3 | `decryptTokenPayload` NEVER throws - returns null on every failure mode | VERIFIED | Entire body wrapped in try/catch; 10+ explicit null-return guards verified in source; 17/17 tests pass including garbage-input, tamper, cross-profile, and truncated-token cases |
| 4 | `isEncryptedToken` returns true only for strings beginning with `mcp4.v1.` | VERIFIED | Implementation: `token.startsWith(TOKEN_PREFIX)`; confirmed by test suite |
| 5 | `deriveTokenKey` returns 32-byte Buffer (hex decode for 64-char hex, SHA-256 otherwise) | VERIFIED | Dual-mode logic confirmed in source; 7 config tests pass covering unset/empty/whitespace/hex/passphrase/trim/all-a paths |
| 6 | `client_secret` is NEVER embedded in envelope | VERIFIED | `grep "client_secret" src/auth/token-envelope.ts` returns 0 matches; `grep "client_secret" src/transport/http-transport.ts | grep -i "creg\|envelope"` returns 0 matches |
| 7 | `HttpTransportConfig` has `tokenKey?: Buffer` field wired from `MCP4_TOKEN_KEY` via `deriveTokenKey()` | VERIFIED | Field at line 105 of `src/types/http-transport.ts`; IIFE parser at lines 41-45 of `http-transport-config.ts` with whitespace trim |
| 8 | `DEFAULT_MAX_TOKEN_LENGTH` raised from 1000 to 4096 | VERIFIED | Line 94: `const DEFAULT_MAX_TOKEN_LENGTH = 4096;`; no `= 1000` present |
| 9 | Constructor warns on missing `tokenKey` | VERIFIED | Lines 165-171: warn emitted when `!this.config.tokenKey`; message contains `MCP4_TOKEN_KEY not set` |
| 10 | `storeOAuthTokens` returns `string` (not void), accepts 5th optional `registeredClient?: OAuthClientInformationFull` param, encrypts when both `tokenKey` and `refresh_token` are present | VERIFIED | Signature confirmed at lines 3936-3942; encryptTokenPayload call at line 3955; catch-block fallback at line 3978 |
| 11 | Both in-memory maps keyed by the returned client token (envelope or raw) | VERIFIED | Lines 3988-4001: `profileState.oauthTokensByAccessToken.set(clientToken, ...)` and `this.inboundAuthTokenStore.store(clientToken, ...)`; old `session.authToken = tokens.access_token` removed |
| 12 | Both grant handlers (auth_code, refresh_token) fetch `registeredClient` and swap `access_token` in response when envelope issued | VERIFIED | Lines 1957-1963 and 1985-1991: `registeredClient = await ...clientsStore.getClient(...)` + conditional `{...tokens, access_token: clientToken}` spread at both sites |
| 13 | Internal `refreshAccessToken` assigns `session.authToken = newClientToken` (return value), not `tokens.access_token` | VERIFIED | Lines 4361-4362: `const newClientToken = this.storeOAuthTokens(...); session.authToken = newClientToken;`; old direct assignment absent (0 matches for `session.authToken = tokens.access_token`) |
| 14 | Session-init fallback decrypts envelope after both Map lookups miss, rehydrates session, re-registers DCR client from creg | VERIFIED | Lines 3050-3094: condition `!internalToken && !refreshToken && this.config.tokenKey && isEncryptedToken(authInfo.token)` gates the fallback; full rehydration of refreshToken/accessTokenExpiresAt/scopes/oauthClientId; creg-driven registerClient with defaults (ru??[], gt??['authorization_code','refresh_token'], rt_??['code'], sc??'') |
| 15 | On envelope decrypt failure, debug log emitted; session continues as plain bearer (no crash, no 401) | VERIFIED | Lines 3089-3091: `this.logger.debug('Encrypted token failed to decrypt ...')` |
| 16 | Cross-profile replay rejected (different `profileId` as AAD) | VERIFIED | AES-GCM AAD mismatch causes tag verification failure -> null return; defense-in-depth `parsed.pid !== profileId` check present; Test P verified |
| 17 | Plain-token backward compat when `MCP4_TOKEN_KEY` unset | VERIFIED | When `!this.config.tokenKey`: startup warn emitted, `storeOAuthTokens` returns raw `tokens.access_token`, session-init fallback branch skipped (`this.config.tokenKey` guard fails) |
| 18 | Documentation updated: env.example, README.md, docs/HTTP-TRANSPORT.md, IMPLEMENTATION.md, CHANGELOG.md | VERIFIED | All 5 docs confirmed; "Encrypted Token Envelopes" section at HTTP-TRANSPORT.md line 611; MCP4_TOKEN_KEY in env.example line 75 and README.md line 568; IMPLEMENTATION.md bullet at line 278; CHANGELOG.md one-line entry at line 11 |

**Score:** 18/18 truths verified

---

### Required Artifacts

| Artifact | Status | Evidence |
|----------|--------|----------|
| `src/auth/token-envelope.ts` | VERIFIED | 203 lines, 5 exports, AES-256-GCM, `mcp4.v1.` prefix, no `client_secret`, no `process.env`, no `upstreamAt`/`upstreamRt` |
| `src/auth/token-envelope.test.ts` | VERIFIED | 226 lines, 17 it-blocks under `describe('token-envelope', ...)`, all 17 pass |
| `src/types/http-transport.ts` | VERIFIED | `tokenKey?: Buffer` field present at line 105 with JSDoc |
| `src/transport/http-transport-config.ts` | VERIFIED | `deriveTokenKey` imported from `../auth/token-envelope.js`; IIFE reads `process.env.MCP4_TOKEN_KEY` exactly once; whitespace trim applied |
| `src/transport/http-transport-config.test.ts` | VERIFIED | `describe('MCP4_TOKEN_KEY')` at line 171; 7 new tests; 12/12 total pass |
| `src/transport/http-transport.ts` | VERIFIED | 4400+ lines; import at line 34; DEFAULT_MAX_TOKEN_LENGTH=4096 at line 94; constructor warn; storeOAuthTokens refactored; 3 call sites updated; session-init fallback block |
| `src/transport/http-transport.test.ts` | VERIFIED | 182/182 tests pass; 19 new tests covering A-R (warn, 4096 boundary, storeOAuthTokens paths, grant responses, session restore, creg variants, cross-profile, missing-tokenKey, plain-bearer) |
| `env.example` | VERIFIED | `MCP4_TOKEN_KEY=` commented entry at line 75 |
| `README.md` | VERIFIED | `MCP4_TOKEN_KEY` entry at line 568; `MCP4_TOKEN_MAX_LENGTH` default updated to 4096 at line 567 |
| `docs/HTTP-TRANSPORT.md` | VERIFIED | "Encrypted Token Envelopes" section at line 611 with format spec, flow, key derivation, rotating-rt limitation, security boundary |
| `IMPLEMENTATION.md` | VERIFIED | Restart-resilient OAuth bullet at line 278 pointing to `src/auth/token-envelope.ts` |
| `CHANGELOG.md` | VERIFIED | One-line entry under `[Unreleased] -> ### Added` at line 11 |

---

### Key Link Verification

| From | To | Via | Status |
|------|----|-----|--------|
| `src/auth/token-envelope.ts` | `node:crypto` | `createCipheriv('aes-256-gcm', key, nonce)` + `setAAD(profileIdBuffer)` | WIRED - confirmed at lines 100-101 |
| `src/auth/token-envelope.ts` | `node:crypto` | `createDecipheriv` with outer try/catch returning null on auth-tag mismatch | WIRED - confirmed at lines 158-162 |
| `src/auth/token-envelope.test.ts` | `src/auth/token-envelope.ts` | `import { TokenEnvelopePayload, deriveTokenKey, isEncryptedToken, encryptTokenPayload, decryptTokenPayload } from './token-envelope.js'` | WIRED - confirmed at line 9 |
| `src/transport/http-transport-config.ts` | `src/auth/token-envelope.ts` | `import { deriveTokenKey } from '../auth/token-envelope.js'` | WIRED - line 7 |
| `buildHttpTransportBaseConfig` | `process.env.MCP4_TOKEN_KEY` | IIFE returning `undefined` when missing/blank, otherwise `deriveTokenKey(raw.trim())` | WIRED - lines 41-45 |
| `HttpTransportConfig.tokenKey` | `Buffer` | `tokenKey?: Buffer` field declaration | WIRED - line 105 of types file |
| `src/transport/http-transport.ts` | `src/auth/token-envelope.ts` | `import { encryptTokenPayload, decryptTokenPayload, isEncryptedToken }` | WIRED - line 34 |
| `storeOAuthTokens` | `encryptTokenPayload` | called when `this.config.tokenKey && tokens.refresh_token`; result used as map key + return value | WIRED - line 3955 |
| Session init fallback | `decryptTokenPayload` | called when both Map lookups missed AND `isEncryptedToken(token)` AND `config.tokenKey` is set | WIRED - lines 3061-3065 |
| Session init fallback | `oauthProvider.clientsStore.registerClient` | called when `envelope.creg` present, guarded by `profileState.oauthProvider` null check | WIRED - lines 3071-3078 |
| `HttpTransport` constructor | `this.logger.warn` | emits 'MCP4_TOKEN_KEY not set' when `config.tokenKey` is undefined | WIRED - lines 165-171 |
| Auth-code grant handler | `response.json` | wraps tokens with `access_token: clientToken` when storeOAuthTokens returns a different (encrypted) string | WIRED - lines 1959-1962 |
| Auth-code + refresh grant | `clientsStore.getClient` | fetches registeredClient for creg embedding (2 call sites) | WIRED - lines 1957 and 1985 |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `storeOAuthTokens` | `tokens.access_token` / `tokens.refresh_token` | `oauthProvider.exchangeAuthorizationCode` / `exchangeRefreshToken` - real IdP exchange | Yes - real OAuth response data | FLOWING |
| `storeOAuthTokens` | `clientToken` (envelope) | `encryptTokenPayload(payload, this.config.tokenKey)` where payload contains live `tokens.access_token`, `tokens.refresh_token`, `expiresAt`, etc. | Yes | FLOWING |
| Session-init fallback | `envelope` | `decryptTokenPayload(authInfo.token, this.config.tokenKey, profileState.profileId)` | Yes - decrypts real envelope or returns null | FLOWING |
| `oauthTokensByAccessToken` / `inboundAuthTokenStore` | keyed by `clientToken` | `profileState.oauthTokensByAccessToken.set(clientToken, ...)` | Yes - keyed by the issued token | FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| 17/17 token-envelope unit tests pass | `npx vitest run src/auth/token-envelope.test.ts` | 17 passed, 0 failed | PASS |
| 12/12 config tests pass | `npx vitest run src/transport/http-transport-config.test.ts` | 12 passed, 0 failed | PASS |
| 182/182 http-transport tests pass | `npx vitest run src/transport/http-transport.test.ts` | 182 passed, 0 failed | PASS |
| Full project typecheck clean | `npm run typecheck` | 0 error TS lines | PASS |
| Full suite (excluding pre-existing failures) | `npm test` | 3488 passed, 3 pre-existing failures in `src/testing/token-validation.test.ts` (documented in deferred-items.md, reproduce on pre-phase main commit) | PASS |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status |
|-------------|------------|-------------|--------|
| DECISION-D-FORMAT | 03.4-01 | `mcp4.v1.<base64url(12-byte-nonce + AES-256-GCM-ciphertext + 16-byte-tag)>` format | SATISFIED |
| DECISION-D-PREFIX | 03.4-01 | `mcp4.v1.` prefix for O(1) detection; passes `validateToken` regex | SATISFIED |
| DECISION-D-KEYDERIV | 03.4-01, 03.4-02 | 64-hex -> Buffer.from hex decode; else SHA-256(raw); whitespace trim | SATISFIED |
| DECISION-D-CREG | 03.4-01, 03.4-03 | creg embedded in envelope; `client_secret` NEVER embedded; re-registered on session restore | SATISFIED |
| DECISION-D-PHASE5-EXT | 03.4-01 | `upstreamAt`/`upstreamRt` NOT in interface (Phase 5 reservation) | SATISFIED |
| DECISION-D-CONFIG-FIELD | 03.4-02 | `HttpTransportConfig.tokenKey?: Buffer` field | SATISFIED |
| DECISION-D-DEFAULT-MAXLEN | 03.4-03 | `DEFAULT_MAX_TOKEN_LENGTH` raised from 1000 to 4096 | SATISFIED |
| DECISION-D-STORE-RETURN | 03.4-03 | `storeOAuthTokens` returns `string` not `void`; 5th param added | SATISFIED |
| DECISION-D-CALLSITES | 03.4-03 | 3 call sites updated; 2 grant handlers swap `access_token`; internal refresh assigns return value | SATISFIED |
| DECISION-D-SESSION-INIT-FALLBACK | 03.4-03 | Decrypt fallback after both Map lookups fail; rehydrates session + creg re-registration | SATISFIED |
| DECISION-D-STARTUP-WARN | 03.4-03 | Constructor warns when `!this.config.tokenKey` | SATISFIED |
| DECISION-D-NOT-CHANGE | 03.4-03 | `refreshAccessToken()` logic, `extractAuthToken()`, `validateToken()`, `SessionData` - all unchanged | SATISFIED |
| DECISION-D-DOCS | 03.4-04 | env.example, README.md, HTTP-TRANSPORT.md, IMPLEMENTATION.md, CHANGELOG.md all updated | SATISFIED |

---

### Anti-Patterns Found

None detected. Specific checks performed:
- No `TODO`/`FIXME`/`PLACEHOLDER` in modified files
- `return null` in `decryptTokenPayload` are intentional error-path returns (not stubs) - data-flow confirmed as real AES-GCM decrypt failures
- No `process.env` in `src/auth/token-envelope.ts` (pure module)
- No `client_secret` in token-envelope.ts or in envelope-related code in http-transport.ts
- No `upstreamAt`/`upstreamRt` (Phase 5 reservation preserved)
- No Unicode em dash or en dash in any new strings (per AGENTS.md)

---

### Human Verification Required

#### 1. Live end-to-end restart resilience

**Test:** Start gateway with `MCP4_TOKEN_KEY=any-passphrase`, complete OAuth flow to obtain an `mcp4.v1.*` token, kill and restart the gateway with the same key, reconnect using the stored token on `initialize`.
**Expected:** Session is restored without re-authentication; info log "Session restored from encrypted token envelope after restart" is visible; tools are callable immediately.
**Why human:** Requires a running IdP, browser OAuth flow, and process restart - cannot be automated with grep/file checks.

#### 2. Startup warn visibility in production log format

**Test:** Deploy with `MCP4_TOKEN_KEY` unset and check log output.
**Expected:** Exactly one warn line at startup containing "MCP4_TOKEN_KEY not set - encrypted token envelopes disabled..." before any request is processed.
**Why human:** Log format and visibility depend on the deployed logging infrastructure.

---

### Gaps Summary

No gaps. All phase must-haves are verified at all 4 levels (exists, substantive, wired, data-flowing).

The 3 pre-existing failures in `src/testing/token-validation.test.ts` are documented in `.planning/phases/03.4-encrypted-token-envelope/deferred-items.md` and confirmed to reproduce on the pre-phase main commit. They are outside this phase's scope.

---

_Verified: 2026-05-06T09:40:00Z_
_Verifier: Claude (gsd-verifier)_
