---
phase: 01-upstream-session-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - src/upstream/upstream-credential-extractor.ts
  - src/upstream/upstream-credential-extractor.test.ts
  - src/upstream/upstream-credential-store.ts
  - src/upstream/upstream-credential-store.test.ts
  - src/upstream/upstream-connection-manager.ts
  - src/upstream/upstream-connection-manager.test.ts
  - src/types/upstream-connection.ts
  - src/types/http-transport.ts
autonomous: true
requirements:
  - PROXY-02
  - SEC-02
gap_closure: true

must_haves:
  truths:
    - "No X-Upstream-Authorization header parsing exists - the extractor module is deleted"
    - "UpstreamCredentialStore class is deleted - no per-provider token map needed"
    - "buildAuthHeaders accepts token: string | undefined directly instead of UpstreamCredentials interface"
    - "UpstreamConnectionManager.getOrConnect accepts token: string | undefined instead of UpstreamCredentials"
    - "SessionData.upstreamCredentials field is removed - existing authToken field is the credential source"
    - "UpstreamCredentials interface is removed from upstream-connection types"
    - "All existing tests that remain pass after refactoring"
  artifacts:
    - path: "src/upstream/upstream-credential-extractor.ts"
      provides: "DELETED"
    - path: "src/upstream/upstream-credential-extractor.test.ts"
      provides: "DELETED"
    - path: "src/upstream/upstream-credential-store.ts"
      provides: "buildAuthHeaders(provider, token) - simplified signature"
      exports: ["buildAuthHeaders"]
    - path: "src/upstream/upstream-credential-store.test.ts"
      provides: "Tests for buildAuthHeaders with simplified token param"
    - path: "src/upstream/upstream-connection-manager.ts"
      provides: "getOrConnect with token: string | undefined param"
    - path: "src/types/upstream-connection.ts"
      provides: "UpstreamConnectionState, UpstreamConnection - no UpstreamCredentials"
  key_links:
    - from: "src/upstream/upstream-connection-manager.ts"
      to: "src/upstream/upstream-credential-store.ts"
      via: "buildAuthHeaders(provider, token)"
      pattern: "buildAuthHeaders"
---

<objective>
Remove dead X-Upstream-Authorization credential delivery code and simplify to direct Authorization header passthrough per profile-per-upstream architecture.

Purpose: UAT Gap 1 (major) - user confirmed every upstream MCP is isolated in its own profile. The client sends `Authorization: Bearer <token>` directly. The multi-provider X-Upstream-Authorization header, UpstreamCredentialStore, extractUpstreamCredentials, and UpstreamCredentials interface are all dead code based on a wrong aggregation-server assumption.

Output: Simplified credential model where buildAuthHeaders accepts a plain token string and getOrConnect takes token: string | undefined. Dead code deleted, all remaining tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-upstream-session-foundation/01-01-SUMMARY.md
@.planning/phases/01-upstream-session-foundation/01-02-SUMMARY.md
@.planning/phases/01-upstream-session-foundation/01-UAT.md

@src/upstream/upstream-credential-extractor.ts
@src/upstream/upstream-credential-extractor.test.ts
@src/upstream/upstream-credential-store.ts
@src/upstream/upstream-credential-store.test.ts
@src/upstream/upstream-connection-manager.ts
@src/upstream/upstream-connection-manager.test.ts
@src/types/upstream-connection.ts
@src/types/http-transport.ts
@src/types/profile.ts

<interfaces>
<!-- Current buildAuthHeaders signature (will be simplified) -->
```typescript
// src/upstream/upstream-credential-store.ts
export function buildAuthHeaders(
  provider: UpstreamMcpServerConfig,
  credentials: UpstreamCredentials,
): Record<string, string>;
```

<!-- Current getOrConnect signature (will be simplified) -->
```typescript
// src/upstream/upstream-connection-manager.ts
async getOrConnect(
  sessionId: string,
  provider: UpstreamMcpServerConfig,
  credentials: UpstreamCredentials,
): Promise<Client>
```

<!-- Current UpstreamCredentials (will be deleted) -->
```typescript
// src/types/upstream-connection.ts
export interface UpstreamCredentials {
  getToken(providerName: string): string | undefined;
  hasCredentials(providerName: string): boolean;
}
```

<!-- SessionData.authToken - the ACTUAL credential source -->
```typescript
// src/types/http-transport.ts
export interface SessionData {
  authToken?: string;  // <-- this is where the client's token lives
  upstreamCredentials?: Map<string, string>;  // <-- DELETE this
  // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete extractor, remove UpstreamCredentials interface, remove SessionData.upstreamCredentials</name>
  <files>
    src/upstream/upstream-credential-extractor.ts,
    src/upstream/upstream-credential-extractor.test.ts,
    src/types/upstream-connection.ts,
    src/types/http-transport.ts
  </files>
  <action>
    This task removes the dead aggregation-model code per UAT Gap 1.

    1. DELETE `src/upstream/upstream-credential-extractor.ts` entirely (file removal via `rm`)
    2. DELETE `src/upstream/upstream-credential-extractor.test.ts` entirely (file removal via `rm`)
    3. Edit `src/types/upstream-connection.ts`:
       - Remove the `UpstreamCredentials` interface entirely (lines 31-35)
       - Keep `UpstreamConnectionState`, `UpstreamConnection`, and all imports unchanged
    4. Edit `src/types/http-transport.ts`:
       - Remove the `upstreamCredentials?: Map<string, string>` field from `SessionData` (line 38-39)
       - Remove the PROXY-02 comment above it
    5. Run typecheck to identify all downstream breakages (expected: upstream-credential-store.ts, upstream-connection-manager.ts, upstream-connection-manager.test.ts)
       - Do NOT fix those yet - Task 2 handles them
  </action>
  <verify>
    <automated>test ! -f src/upstream/upstream-credential-extractor.ts && test ! -f src/upstream/upstream-credential-extractor.test.ts && ! grep -q "UpstreamCredentials" src/types/upstream-connection.ts && ! grep -q "upstreamCredentials" src/types/http-transport.ts && npm run typecheck 2>&1 | grep -E "error TS" | grep -v "upstream-credential-store\|upstream-connection-manager" | (! grep -q ".") && echo "PASS"</automated>
  </verify>
  <done>
    Extractor files deleted. UpstreamCredentials interface removed. SessionData.upstreamCredentials field removed. Typecheck confirms only expected downstream breakages in upstream-credential-store.ts and upstream-connection-manager.ts (fixed in Task 2). No unexpected compile errors in unrelated files.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Simplify buildAuthHeaders and getOrConnect to accept token: string | undefined</name>
  <files>
    src/upstream/upstream-credential-store.ts,
    src/upstream/upstream-credential-store.test.ts,
    src/upstream/upstream-connection-manager.ts,
    src/upstream/upstream-connection-manager.test.ts
  </files>
  <behavior>
    - buildAuthHeaders(provider, 'mytoken') with bearer auth returns { Authorization: 'Bearer mytoken' }
    - buildAuthHeaders(provider, 'mytoken') with custom-header auth returns { 'X-Api-Key': 'mytoken' }
    - buildAuthHeaders(provider, undefined) returns {}
    - buildAuthHeaders(provider with no auth, 'tok') returns {}
    - getOrConnect(sessionId, provider, 'mytoken') creates connection with correct auth headers
    - getOrConnect(sessionId, provider, undefined) creates connection with empty auth headers
    - All existing connection manager behaviors (dedup, closeAll, error mapping) remain unchanged
  </behavior>
  <action>
    1. Refactor `src/upstream/upstream-credential-store.ts`:
       - DELETE the `UpstreamCredentialStore` class entirely
       - Remove the import of `UpstreamCredentials` from types
       - Change `buildAuthHeaders` signature from `(provider: UpstreamMcpServerConfig, credentials: UpstreamCredentials)` to `(provider: UpstreamMcpServerConfig, token: string | undefined)`
       - Update implementation: replace `credentials.getToken(provider.name)` with the `token` parameter directly
       - The file now exports ONLY `buildAuthHeaders`
       - Rename the file to `src/upstream/upstream-auth-headers.ts` would be cleaner, but to minimize churn, keep the filename. Add a comment at top: "Auth header builder for upstream MCP provider requests. Profile-per-upstream model: one token per session."

    2. Rewrite `src/upstream/upstream-credential-store.test.ts`:
       - Remove ALL UpstreamCredentialStore tests (the class no longer exists)
       - Keep and update `buildAuthHeaders` tests:
         - `buildAuthHeaders(bearerProvider, 'tok')` returns `{ Authorization: 'Bearer tok' }`
         - `buildAuthHeaders(customHeaderProvider, 'tok')` returns `{ 'X-Api-Key': 'tok' }`
         - `buildAuthHeaders(queryProvider, 'tok')` returns `{}`
         - `buildAuthHeaders(bearerProvider, undefined)` returns `{}`
         - `buildAuthHeaders(noAuthProvider, 'tok')` returns `{}`
       - Tests should construct provider configs inline (no store needed)

    3. Refactor `src/upstream/upstream-connection-manager.ts`:
       - Remove import of `UpstreamCredentials` from `../types/upstream-connection.js`
       - Change `getOrConnect` signature: replace `credentials: UpstreamCredentials` with `token: string | undefined`
       - Change `createConnection` signature: replace `credentials: UpstreamCredentials` with `token: string | undefined`
       - Update `createConnection` body: call `buildAuthHeaders(provider, token)` instead of `buildAuthHeaders(provider, credentials)`

    4. Refactor `src/upstream/upstream-connection-manager.test.ts`:
       - Remove import of `UpstreamCredentials`
       - Remove `createCredentials()` helper function
       - Replace all `createCredentials()` calls in getOrConnect with a plain string like `'test-token'` or `undefined`
       - All existing test assertions remain the same - only the parameter shape changes
  </action>
  <verify>
    <automated>npx vitest run src/upstream/upstream-credential-store.test.ts src/upstream/upstream-connection-manager.test.ts && npm run typecheck</automated>
  </verify>
  <done>
    buildAuthHeaders accepts (provider, token: string | undefined). UpstreamCredentialStore deleted. getOrConnect accepts token: string | undefined. All tests pass. Typecheck passes. No references to UpstreamCredentials interface remain in the codebase.
  </done>
</task>

</tasks>

<verification>
npx vitest run src/upstream/ src/auth/ && npm run typecheck && ! grep -r "UpstreamCredentials" src/ && ! grep -r "extractUpstreamCredentials" src/ && ! grep -r "UPSTREAM_AUTH_HEADER" src/ && ! grep -r "upstreamCredentials" src/types/http-transport.ts
</verification>

<success_criteria>
- upstream-credential-extractor.ts and its test file are deleted
- UpstreamCredentials interface no longer exists anywhere in src/
- UpstreamCredentialStore class no longer exists
- SessionData.upstreamCredentials field removed
- buildAuthHeaders accepts (provider, token: string | undefined)
- getOrConnect accepts (sessionId, provider, token: string | undefined)
- All remaining upstream tests pass
- Typecheck passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-upstream-session-foundation/01-04-SUMMARY.md`
</output>
