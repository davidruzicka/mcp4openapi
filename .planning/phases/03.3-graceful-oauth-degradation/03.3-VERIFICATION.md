---
phase: 03.3-graceful-oauth-degradation
verified: 2026-05-05T13:07:00Z
status: passed
score: 12/12 must-haves verified
re_verification: false
---

# Phase 03.3: Graceful OAuth Degradation Verification Report

**Phase Goal:** When an OAuth profile is loaded but its config is incomplete (missing env vars, missing redirect_uri), the server must not crash, must not send a 401 OAuth challenge, and must not show the OAuth tab on the HTML index page.
**Verified:** 2026-05-05T13:07:00Z
**Status:** passed
**Re-verification:** No - initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `isOAuthConfigOperational()` returns operational=false when issuer and both endpoints are absent | VERIFIED | Lines 77-96 of `src/auth/oauth-provider.ts`; 11 parametrized tests in oauth-provider.test.ts all pass |
| 2 | `isOAuthConfigOperational()` returns operational=false when redirect_uri is missing and allow_unregistered_clients is not true | VERIFIED | Lines 90-93 of `src/auth/oauth-provider.ts`; test case "operational=false when only issuer provided" passes |
| 3 | `isOAuthConfigOperational()` returns operational=false when a referenced env var is not set | VERIFIED | Lines 56-57 of `src/auth/oauth-provider.ts`; env-var isolation tests pass (vi.stubEnv pattern) |
| 4 | `isOAuthConfigOperational()` returns operational=true when allow_unregistered_clients=true and redirect_uri is absent | VERIFIED | Lines 90-93 (condition skipped when allow_unregistered_clients truthy); dedicated test passes |
| 5 | `isOAuthConfigOperational()` returns operational=true for a complete config with resolved env vars | VERIFIED | Multiple test cases confirm operational=true for complete configs |
| 6 | Profile index page does not show OAuth auth tab when OAuth config is not operational | VERIFIED | `extractAuthMethods()` in `src/profile/profile-resolver.ts` lines 288-293 skips OAuth entries via `continue` when !operational; test at profile-resolver.test.ts:883 and profile-index.test.ts:890 pass |
| 7 | MCP initialize request succeeds without 401 OAuth challenge when OAuth config is not operational | VERIFIED | Auth gate at http-transport.ts:2802 guards with `!profileState.oauthDisabledReason` before sending 401 challenge |
| 8 | Server does not crash with 500 when ExternalOAuthProvider constructor would throw due to unresolved env vars | VERIFIED | `getProfileState()` runs pre-flight check before constructor call (http-transport.ts:502); try/catch around constructor for race edge case (lines 510-520) |
| 9 | oauthDisabledReason is set in ProfileRuntimeState when OAuth is degraded | VERIFIED | Field declared at http-transport.ts:105; set at lines 504, 519; stored in state literal at line 549 |
| 10 | oauthProvider is null in ProfileRuntimeState when OAuth is degraded | VERIFIED | When !operational branch taken, `oauthProvider` remains null (initialized at line 497); confirmed by http-transport.test.ts:3138 test |
| 11 | Warning is logged once with profileId and reason when OAuth is degraded | VERIFIED | `logger.warn('OAuth config not operational - OAuth disabled for profile', { profileId, reason })` at lines 523-527 |
| 12 | Complete OAuth config behavior is unchanged | VERIFIED | All 3430 existing tests pass; fixtures updated to add redirect_uri where missing, preserving correct OAuth enforcement |

**Score:** 12/12 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/auth/oauth-provider.ts` | Exports `isOAuthConfigOperational` and `OAuthOperationalCheck`; module-private `tryResolveEnvRef` | VERIFIED | Lines 54-96; both exports present, tryResolveEnvRef has no `export` keyword |
| `src/auth/oauth-provider.test.ts` | Unit tests covering all isOAuthConfigOperational branches | VERIFIED | 11 parametrized test cases under `describe('isOAuthConfigOperational')`; all pass |
| `src/transport/http-transport.ts` | `oauthDisabledReason` field in ProfileRuntimeState, pre-flight check in `getProfileState()`, auth gate guard | VERIFIED | 8 occurrences of `oauthDisabledReason`; pre-flight at line 502; auth gate at line 2802 |
| `src/profile/profile-resolver.ts` | OAuth filtering in `extractAuthMethods()` when config is not operational | VERIFIED | Lines 288-293; `isOAuthConfigOperational` called and `continue` skips non-operational OAuth entries |
| `src/profile/profile-resolver.test.ts` | Test: OAuth filtered from authMethods when env var missing; test: OAuth kept when complete | VERIFIED | Tests at lines 883 and 927; both pass |
| `src/transport/profile-index.test.ts` | Test: OAuth authTab absent when authMethods has no oauth entry | VERIFIED | Test at line 890 passes |
| `src/transport/http-transport.test.ts` | Test: getProfileState sets oauthDisabledReason and null oauthProvider for incomplete config | VERIFIED | Test at line 3113; assertions at lines 3138-3139 confirm oauthDisabledReason contains 'redirect_uri' |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `src/profile/profile-resolver.ts` | `src/auth/oauth-provider.ts` | `import { isOAuthConfigOperational }` | WIRED | Line 15: `import { isOAuthConfigOperational } from '../auth/oauth-provider.js'` |
| `src/transport/http-transport.ts` | `src/auth/oauth-provider.ts` | `import { ExternalOAuthProvider, isOAuthConfigOperational }` | WIRED | Line 33: both symbols imported in single named import |
| `getProfileState` (http-transport.ts) | `ProfileRuntimeState.oauthDisabledReason` | pre-flight check before ExternalOAuthProvider constructor | WIRED | Lines 502-549; pre-flight sets `oauthDisabledReason` variable, stored in state literal |
| auth gate (http-transport.ts line 2802) | `ProfileRuntimeState.oauthDisabledReason` | `!profileState.oauthDisabledReason` guard | WIRED | Exact pattern `!profileState.oauthDisabledReason` at line 2802 |

---

### Data-Flow Trace (Level 4)

Not applicable. No dynamic data rendering components introduced. All artifacts are utility functions, runtime state, and routing logic — no UI data pipeline to trace.

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `isOAuthConfigOperational` exports present | `grep "export.*isOAuthConfigOperational\|export.*OAuthOperationalCheck" src/auth/oauth-provider.ts` | 2 matches found | PASS |
| `tryResolveEnvRef` is not exported (module-private) | `grep "^export.*tryResolveEnvRef" src/auth/oauth-provider.ts` | 0 matches (no export) | PASS |
| Auth gate guard present | `grep "!profileState\.oauthDisabledReason" src/transport/http-transport.ts` | 1 match at line 2802 | PASS |
| `oauthDisabledReason` in ProfileRuntimeState interface | `grep "oauthDisabledReason" src/transport/http-transport.ts` | 8 occurrences across interface, getProfileState, auth gate, state literal | PASS |
| Full test suite | `npm test` | 3430/3430 passed, 158 test files | PASS |
| TypeScript typecheck | `npm run typecheck` | 0 errors | PASS |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| DEGRADE-01 | 03.3-01-PLAN.md, 03.3-02-PLAN.md | Graceful OAuth degradation: no crash, no 401 challenge, no OAuth tab when config incomplete | SATISFIED | Pre-flight check (`isOAuthConfigOperational`) gates all three failure modes; wired into profile-resolver (HTML index) and http-transport (runtime auth gate); 3430 tests pass |

**Note on REQUIREMENTS.md:** DEGRADE-01 does not appear in `.planning/REQUIREMENTS.md`. It is a phase-local requirement defined only in ROADMAP.md for this inserted tactical fix phase. This is consistent with the ROADMAP entry which notes "Requirements: DEGRADE-01" inline and does not cross-reference the shared requirements table. No orphaned requirements were found — the REQUIREMENTS.md traceability table covers Phases 1-5 only (v1 milestone requirements), and Phase 03.3 is an inserted sub-phase with its own self-contained acceptance criteria.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (none) | - | - | - | - |

No TODO/FIXME/placeholder comments found in modified source files. No empty implementations, no hardcoded stubs.

---

### Human Verification Required

None. All observable behaviors were verified programmatically:
- Function correctness verified by 11 parametrized unit tests
- Auth gate behavior verified by http-transport test asserting oauthProvider=null and oauthDisabledReason set
- HTML index filtering verified by profile-index test asserting no oauth authTab
- Full regression check via 3430-test suite

---

### Gaps Summary

No gaps. All must-haves verified at all levels (exists, substantive, wired).

---

_Verified: 2026-05-05T13:07:00Z_
_Verifier: Claude (gsd-verifier)_
