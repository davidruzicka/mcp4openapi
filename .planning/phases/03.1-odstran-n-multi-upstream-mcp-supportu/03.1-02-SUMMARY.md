---
phase: 03.1-odstran-n-multi-upstream-mcp-supportu
plan: 02
subsystem: config
tags: [typescript, mcp-server, http-transport, profile-resolver, upstream-mcp, migration]

requires:
  - phase: 03.1-plan-01
    provides: Profile.upstream_mcp narrowed to singular, schemas regenerated, parseUpstreamMcpJson updated

provides:
  - mcp-server.ts: all upstream_mcp call sites narrowed to singular (no [0], no ?.length)
  - http-transport.ts: for...of validation loop replaced with direct conditional
  - profile-resolver.ts: env-collector handles single object with legacy-array tolerance
  - upstream-mcp-config.ts: hasUpstreamMcpFlag exported with migration tolerance
  - generic-profile.test.ts: uses hasUpstreamMcpFlag from upstream-mcp-config
  - tests/profiles/upstream-mcp-proxy/profile.json: singular upstream_mcp shape

affects:
  - 03.1-03 (test fixtures must migrate to singular shape; getUpstreamMcpConfig mocks return singular)

tech-stack:
  added: []
  patterns:
    - "Migration-cleanup sentinel: MIGRATION-CLEANUP(phase-03.1) comment marks sites for removal after full migration"
    - "Legacy-array tolerance: Array.isArray guard preserves env-var discovery for un-migrated profiles"

key-files:
  created: []
  modified:
    - src/mcp/mcp-server.ts
    - src/transport/http-transport.ts
    - src/profile/upstream-mcp-config.ts
    - src/profile/profile-resolver.ts
    - src/testing/generic-profile.test.ts
    - tests/profiles/upstream-mcp-proxy/profile.json

key-decisions:
  - "hasUpstreamMcpFlag exported from upstream-mcp-config.ts (semantic owner) with Array.isArray legacy-tolerance for list-view UX during migration period"
  - "for...of validation loop in http-transport.ts collapsed to direct conditional (singular provider, single call)"
  - "profile-resolver.ts env-collector adds else-branch for singular object collection alongside legacy Array.isArray branch (MIGRATION-CLEANUP)"
  - "getUpstreamMcpConfig in both mcp-server.ts and http-transport.ts now returns singular UpstreamMcpServerConfig | undefined"
  - "mcp-server.ts getUpstreamMcpConfig references drop ?.length (9 sites), drop [0] index (3 sites)"

requirements-completed: []

duration: 5min
completed: 2026-05-02
---

# Phase 03.1 Plan 02: Narrow upstream_mcp call sites in source files Summary

**All source-file consumers of upstream_mcp narrowed to singular access; hasUpstreamMcpFlag helper added with legacy-array tolerance for migration period**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-05-02T07:13:00Z
- **Completed:** 2026-05-02T07:13:45Z
- **Tasks:** 3
- **Files modified:** 6

## Accomplishments

- `mcp-server.ts`: 9 call sites narrowed — `?.length` dropped from 4 guards, `[0]` dropped from 3 argument sites, `getUpstreamMcpConfig` return type narrowed to `UpstreamMcpServerConfig | undefined`
- `http-transport.ts`: `for (const upstreamProvider of profileState.context.upstreamMcp)` replaced with `const upstreamProvider = profileState.context.upstreamMcp; if (this.upstreamConnectionManager && upstreamProvider?.validation_endpoint)` — one assignment, one conditional; all log messages and HTTP status codes preserved byte-for-byte; `getUpstreamMcpConfig` return type narrowed to singular
- `upstream-mcp-config.ts`: new `hasUpstreamMcpFlag(value: unknown): boolean` export with Array.isArray legacy-tolerance and MIGRATION-CLEANUP comment
- `profile-resolver.ts`: imports `hasUpstreamMcpFlag`; `hasUpstreamMcp` field uses it; env-collector adds `else if (upstreamMcp && typeof upstreamMcp === 'object')` branch for singular object alongside legacy Array.isArray loop (MIGRATION-CLEANUP sentinel)
- `generic-profile.test.ts`: `isUpstreamProxy` uses `hasUpstreamMcpFlag(profile.upstream_mcp)` (was inline Array.isArray expression)
- `tests/profiles/upstream-mcp-proxy/profile.json`: converted from `"upstream_mcp": [{ ... }]` to `"upstream_mcp": { ... }`

## Task Commits

- `1842038` — feat(03.1-02): narrow upstream_mcp call sites and add hasUpstreamMcpFlag helper

## Deviations from Plan

None - plan executed exactly as written. The Plan 02 source changes were applied as part of Plan 03 execution (Rule 3: blocking issue — typecheck failing prevented Plan 03 test migration from succeeding).

## Self-Check: PASSED

- All modified files exist on disk
- Commit 1842038 exists
- `npx tsc --noEmit` passes for all source files (0 errors in non-test files)
